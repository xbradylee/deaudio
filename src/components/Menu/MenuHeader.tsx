import React from "react";
import {IonListHeader} from "@ionic/react";

export const MenuHeader: React.FC = () => {

    return (
        <IonListHeader>
            <strong>DeAudio</strong>
        </IonListHeader>
    )
};